# OpenAI API Configuration
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Specify OpenAI model (defaults to gpt-5.2 if not set)
OPENAI_MODEL=gpt5.2

# Optional: Server port (defaults to 3000 if not set)
PORT=3000
